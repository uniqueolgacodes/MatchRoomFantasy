// Placeholder Supabase database types.
// Generate the real types once your schema is pushed:
//   supabase gen types typescript --local > types/index.ts
// (or --project-id <ref> for a linked remote project)
export type Database = any;
